# EPAM Web Tests

This repository contains a Playwright test that navigates EPAM website to validate the Client Work page flow.

Run locally:

1. npm ci
2. npx playwright install
3. npx playwright test
